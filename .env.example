# Ultamail / OX App Suite SMTP. Copy to .env.local and fill in. Do not commit .env.local.
# Quote SMTP_PASS if it contains # ! or spaces, e.g. SMTP_PASS="your-pass"
SMTP_HOST=smtp.us.appsuite.cloud
SMTP_PORT=587
SMTP_USER=hello@genixalab.com
SMTP_PASS=
